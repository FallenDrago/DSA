

class Solution {
    static int searchInSorted(int arr[], int N, int K) {
        
        int target=K;
        for(int i=0;i<arr.length;i++){
            if(arr[i]==target){
                return 1;
            }
        }
        return -1;
    }
}
